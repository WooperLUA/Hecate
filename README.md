# Hecate

**Hecate** is a minimal JavaScript router.  
It allows you to redirect pages simply by adding attributes to buttons or other clickable elements.

---

## 🌟 Features

- Redirect to any page using **`hc-gate`**  
- Optional file extension via **`hc-extension`** (defaults to `.html`)  
- Works with **dynamically created elements**  
- Minimal, lightweight and plug-and-use.
- No dependencies  

---

## ⚡ Installation (CDN)

Include the script in your HTML:

```html
<script src="https://cdn.jsdelivr.net/gh/WooperLUA/hecate@master/Hecate.js"></script>
